import { StandardMarkerIcon } from './standard-marker-icon';

describe('StandardMarkerIcon', () => {
  it('should create an instance', () => {
    expect(new StandardMarkerIcon()).toBeTruthy();
  });
});
